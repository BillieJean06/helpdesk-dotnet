# Copie para .env (ignorado pelo git) e ajuste se não usar o docker-compose.yml como está.
# Usadas pelo dotnet ef (linha de comando), não pela API — a API lê de user-secrets.

HELPDESK_DEV_CONNECTION=Host=localhost;Port=5432;Database=helpdesk_dev;Username=helpdesk;Password=helpdesk
HELPDESK_TEST_CONNECTION=Host=localhost;Port=5432;Database=postgres;Username=helpdesk;Password=helpdesk
